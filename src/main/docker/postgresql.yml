version: '2'
services:
  blog-postgresql:
    image: postgres:12.1
    # volumes:
    #     - ~/volumes/jhipster/Blog/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Blog
      - POSTGRES_PASSWORD=
    ports:
      - 5432:5432
